# 蓝牙配网（BluFi）任务清单

> 配套文档：`docs/蓝牙配网详细设计.md`（V2：无 ESP32，AIC8800 BLE + BlueZ D-Bus）。**编码级接口规格见详细设计 §4.6**（帧常量 / Type 字节换算 / 分片 / codec / wifi / gatt / mod 主循环），实现前必读。
> 实现范围：**仅 `rv1106-mqtt-client/`**
> 状态：⬜ 待办　🟨 进行中　✅ 完成　⛔ 阻塞（受前置项制约）
> 更新日期：2026-09-05

---

## P0 前置排查（阻塞后续，需登板或抓包确认）

| # | 任务 | 排查方法 | 验收标准 | 状态 |
|---|---|---|---|---|
| ~~P0-1~~ | ✅ **已确认：本地无 `0xFFFF` 服务**（`bluetoothctl show` 仅 7 个标准 UUID + 厂商 NUS；`GetManagedObjects` 无 `GattService1`）→ **我们自主注册，无冲突**。广播名规则定为 `{mqtt.model}-{device.id}`（实测现状 `M1S-Ge33700a6620dfddc`）。遗留小项：NUS demo 进程身份待查（确认是否抢占广播 LocalName） | **注意：板子无 `btmon`（返回 127），抓包不可用**，改用以下替代：①`bluetoothctl show` 看输出中 `UUIDs:` 是否含 `0000ffff-...`（最快）；②`dbus-send --system --dest=org.bluez --print-reply / org.freedesktop.DBus.ObjectManager.GetManagedObjects \| grep -iE "ffff\|Service1\|Characteristic1"`（最可靠，无需额外工具）；③`kill -USR2 667` 开 bluetoothd debug 日志（等同抓包，会打印 ATT 读写细节）；④手机装 nRF Connect 直接扫服务列表；⑤`tr '\0' ' ' < /proc/667/cmdline`、`ps \| grep -iE 'blue\|bt\|aic\|gatt'`、`busctl --system list \| grep -iE 'blue\|bt\|aic'`、`ls /usr/libexec/bluetooth/plugins/` | 明确「是否已有 0xFFFF」「注册者是谁」，给出「接替 or 复用」决策。**已知**：bluetoothd PID 667 运行、本地 database 接受 ATT 连接、MTU=517、APP 连接保持约 32.7 s（推断服务发现已成功）。**注意：本项不阻塞阶段 1/2/3**，即使撞车也只会在 `RegisterApplication` 时报错，回头处理即可 | ⬜ |
| P0-2 | **确认 BluFi 安全模式档位** | 设备侧先按明文实现，收到控制帧 `0x1` 时打印 `data[0]`（高 4 位=控制帧模式，低 4 位=数据帧模式）；或用手机 BLE 抓包 | 明确为 L0 明文 / L1 CRC16 / L2 AES / L3 DH+AES（依据 APP 未调用 `negotiateSecurity()`，预判 L0） | ⬜ |
| P0-3 | 确认 BlueZ 版本与 D-Bus 可用性 | `bluetoothd -v`；确认 `org.bluez.GattManager1`、`org.bluez.LEAdvertisingManager1` 存在 | 版本 ≥ 5.40，两个 Manager 接口可用（**实测 bluetoothd 已运行 PID 667，见详细设计 §4.3.1，说明 BlueZ 已装，仅需确认版本号**） | ⬜ |
| P0-4 | 确认系统是否带 `libdbus` 及交叉编译头文件 | 在 SDK sysroot 中查找 `libdbus-1.so`、`dbus/dbus.h` | 能链接；否则走 FFI 或命令行回退方案 | ⬜ |
| P0-5 | 确认 `wpa_cli` 参数 | `wpa_cli -i wlan0 status`；确认接口名与 ctrl 路径 | 给出实际 `iface` 与 `wpa_ctrl` 值（**板子运行 wpa_supplicant 已知，主要确认接口名，默认 wlan0**） | ⬜ |
| P0-6 | 确认 RSSI 编码口径 | 抓包比对一个已知热点（如 `-35`） | 明确按 `i8` 原值还是绝对值 | ⬜ |

---

## 阶段 1：骨架与配置（不依赖 P0）

| # | 任务 | 文件 | 验收标准 | 状态 |
|---|---|---|---|---|
| 1-1 | 新增 `blufi` 模块骨架（`mod.rs` + 空 `BluFiWorker`） | `src/blufi/mod.rs`、`src/lib.rs` | `cargo build` 通过 | ⬜ |
| 1-2 | `BluFiConfig` 定义 + 默认值 + 校验（`scan_timeout_ms < 10000`） | `src/blufi/config.rs`、`src/config.rs` | 缺省可加载；非法值报错 | ⬜ |
| 1-3 | `config/mqtt-client.toml` 增加 `[blufi]` 段 | `config/mqtt-client.toml` | 旧配置文件不改也能启动 | ⬜ |
| 1-4 | `BlufiCmd` / `BlufiEvent` 通道定义 | `src/blufi/mod.rs` | 可通过通道收发 | ⬜ |
| 1-5 | `main.rs` 装配 `blufi` 线程（先只打日志） | `src/main.rs` | 日志出现 `blufi thread started`；不影响现有 MQTT | ⬜ |

---

## 阶段 2：BluFi 协议栈（纯逻辑，可先做，便于单测）

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 2-1 | 帧结构定义与编解码（控制帧/数据帧、Sequence） | `src/blufi/frame.rs` | 单测：编解码互逆、非法输入不 panic | — | ⬜ |
| 2-2 | 分片重组（置位 `0x10`，Data 前 2 字节为总长度） | `src/blufi/frame.rs` | 单测：多片重组、乱序/丢片处理 | 2-1 | ⬜ |
| 2-3 | 发送分片（按 MTU 切分） | `src/blufi/frame.rs` | 单测：边界长度正确切分 | 2-1 | ⬜ |
| 2-4 | ACK 处理（收到 `0x08` 请求时回 `0x0`，data=被确认帧 Sequence） | `src/blufi/frame.rs` | 单测：Sequence 回显正确 | 2-1 | ⬜ |
| 2-5 | 安全模式框架（`SecurityMode` 枚举 + L0 明文实现 + L1~L3 预留接口） | `src/blufi/security.rs` | 收到 `0x1` 能记录模式并回 ACK；非 L0 时显式告警 | 2-1、P0-2 | ⬜ |
| 2-6 | CRC16 实现（若 P0-2 判定需要） | `src/blufi/security.rs` | 单测与已知向量比对 | P0-2 | ⬜ |
| 2-7 | AES / DH 实现（若 P0-2 判定需要） | `src/blufi/security.rs` | 与 APP 抓包数据互通 | P0-2 | ⬜ |

---

## 阶段 3：`wpa_cli` 封装

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 3-1 | 命令执行封装（`Command::new("wpa_cli")` + `-i` + 超时） | `src/blufi/wifi.rs` | 捕获 stdout/stderr 与非零退出码 | — | ⬜ |
| 3-2 | 扫描：`scan` + 轮询 `scan_results`（总超时 `scan_timeout_ms`） | `src/blufi/wifi.rs` | 返回 `(ssid, rssi)`；超时可控 | 3-1 | ⬜ |
| 3-3 | `scan_results` 解析（跳表头、SSID 含空格、隐藏网络丢弃） | `src/blufi/wifi.rs` | 单测覆盖三类边界 | 3-2 | ⬜ |
| 3-4 | 配网：将 ssid/pwd 写入 `/etc/wpa_supplicant.conf`（单一 network 块，有密码 `psk=` / 无密码 `key_mgmt=NONE`），新增或修改时 `wpa_cli reconfigure`（失败回退 `systemctl restart wpa_supplicant`）重载；轮询 `status` 等 IP；已存在且密码一致不重启 | `src/blufi/wifi.rs` | 配置持久化、重启自连；重复配置幂等 | 3-1 | ⬜ |
| 3-5 | 连接状态轮询（`status` → `wpa_state=COMPLETED` + `ip_address`） | `src/blufi/wifi.rs` | 成功/超时/失败三态明确 | 3-4 | ⬜ |
| 3-6 | 失败清理：`remove_network <id>` | `src/blufi/wifi.rs` | 失败后配置不残留 | 3-4 | ⬜ |
| 3-7 | 启动探测 `wpa_cli` 可用性，不可用则禁用配网并告警 | `src/blufi/wifi.rs` | 缺 `wpa_cli` 时 MQTT 仍正常 | 3-1 | ⬜ |

---

## 阶段 4：BlueZ D-Bus 传输层

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 4-1 | 引入 D-Bus 依赖（`dbus` 优先，评估交叉编译） | `crates/mqtt-client/Cargo.toml` | 交叉编译通过 | P0-4 | ⬜ |
| 4-2 | 连接 system bus、确认 `org.bluez`、取 adapter 路径 | `src/blufi/gatt.rs` | 能拿到 `/org/bluez/hciX` 并读到 Powered | 4-1 | ⬜ |
| 4-3 | 实现 `ObjectManager` + `GattApplication1` + `GattService1(0xFFFF)` | `src/blufi/gatt.rs` | `bluetoothctl` 能看到服务 | 4-2 | ⬜ |
| 4-4 | 实现 `GattCharacteristic1`：`0xFF01`(write) / `0xFF02`(notify) | `src/blufi/gatt.rs` | 能看到两个特征及 flags | 4-3 | ⬜ |
| 4-5 | `GattManager1.RegisterApplication` / `UnregisterApplication` | `src/blufi/gatt.rs` | 注册成功；退出时清理干净 | 4-4 | ⬜ |
| 4-6 | 接收写入：`WriteValue` → 字节交给 `frame.rs` | `src/blufi/gatt.rs` | 手机写入能收到原始字节 | 4-4 | ⬜ |
| 4-7 | 发送通知：`0xFF02` 经 `PropertiesChanged` 发 `Value` | `src/blufi/gatt.rs` | 手机端能收到通知 | 4-4 | ⬜ |
| 4-8 | 广播注册：`LEAdvertisingManager1`，`LocalName = {model}-<deviceId>`（model 取 `[mqtt] model`，如 `M1S-Ge33700a6620dfddc`） | `src/blufi/gatt.rs` | APP 能搜到该名称 | 4-2、P0-1 | ⬜ |
| 4-9 | 连接/断开事件处理（APP 断连后回到 WaitApp 并清理状态） | `src/blufi/gatt.rs` | 断连后可重新被连接 | 4-6 | ⬜ |
| 4-10 | D-Bus 异常重连（退避 1s→2s→4s…上限 30s） | `src/blufi/gatt.rs` | `bluetoothd` 重启后能自恢复 | 4-2 | ⬜ |

---

## 阶段 5：状态机与业务编排

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 5-1 | 主循环与状态机骨架（Init/RegisterGatt/WaitApp/…） | `src/blufi/mod.rs` | 状态迁移日志清晰 | 1-5、2-1 | ⬜ |
| 5-2 | 配网串解析（`SSID:/PWD:/IP:/PORT:`） | `src/blufi/codec.rs` | 单测：完整 / 缺可选字段 / 异常输入 | — | ⬜ |
| 5-3 | 热点列表编码（长度 + RSSI + SSID，含分片） | `src/blufi/codec.rs` | 单测：字节布局与分片边界正确 | P0-6 | ⬜ |
| 5-4 | 扫描流程接线（`0x9` → `wpa_cli` → 编码 → `0x11` 通知） | `src/blufi/mod.rs` | APP 10 s 内看到列表；空列表也回 | 3-2、4-7、5-3 | ⬜ |
| 5-5 | 回执发送（`Received SSID and password` ×3，间隔 500ms） | `src/blufi/mod.rs` | 抓包确认 3 帧 | 5-2、4-7 | ⬜ |
| 5-6 | 配网流程接线（回执 → `wpa_cli` 配网 → 等 IP） | `src/blufi/mod.rs` | 能连上 AP 并拿到 IP | 3-4、5-5 | ⬜ |
| 5-7 | 成功上报（数据帧 `0xF`：opmode=STA、状态=已连有 IP、SSID） | `src/blufi/mod.rs` | APP 显示配网成功 | 5-6 | ⬜ |
| 5-8 | 失败处理（`Wifi connection failed` ×3 → 回 WaitApp） | `src/blufi/mod.rs` | 错误密码场景符合预期 | 5-6 | ⬜ |
| 5-9 | 幂等处理（APP 重发 / 用户重试同一配置） | `src/blufi/mod.rs` | 重复下发无副作用 | 5-6 | ⬜ |
| 5-10 | 版本响应（数据帧 `0x10`）与错误上报（`0x12`/`0x0b`） | `src/blufi/mod.rs` | APP 请求版本有响应；扫描失败有上报 | 4-7 | ⬜ |

---

## 阶段 6：与主流程协同

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 6-1 | `BlufiEvent` 上报与主线程日志记录 | `src/main.rs` | 配网成功/失败有明确日志 | 1-4、5-7 | ⬜ |
| 6-2 | 启动即未联网时自动进入配网模式 | `src/blufi/mod.rs` | 无网络时 APP 能搜到蓝牙 | 4-8 | ⬜ |
| 6-3 | `BlufiCmd::StartConfig` 支持重新配网（预留云端解绑） | `src/blufi/mod.rs`、`src/main.rs` | 收到命令后能重新广播 | 5-1 | ⬜ |

---

## 阶段 7：测试与文档

| # | 任务 | 文件 | 验收标准 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 7-1 | `frame.rs` 单测（编解码、分片、ACK、非法输入） | `src/blufi/frame.rs` | `cargo test` 通过 | 2-4 | ⬜ |
| 7-2 | `codec.rs` 单测（解析 + 编码 + 分片边界） | `src/blufi/codec.rs` | `cargo test` 通过 | 5-3 | ⬜ |
| 7-3 | 协议回放测试（录制帧序列驱动状态机） | `src/blufi/mod.rs` | 覆盖成功 / 失败 / 重发三条路径 | 5-10 | ⬜ |
| 7-4 | 真机联调：扫描 → 下发 → 联网 → MQTT 上线 | — | 端到端跑通 | 5-7 | ⬜ |
| 7-5 | 异常用例（无 bluetoothd、`wpa_cli` 缺失、扫描无结果、重复下发、中途断连） | — | 均不崩溃且可恢复 | 7-4 | ⬜ |
| 7-6 | 协议文档补「设备侧实现说明」（记录 P0-2 安全模式、P0-6 RSSI 口径结论） | `M2 APP 蓝牙通信（BluFi）协议与指令清单.md` | 结论落文档 | P0-2、P0-6 | ⬜ |
| 7-7 | 更新项目文档（概要/详细设计增补 blufi 线程与 GATT 应用） | `docs/概要设计说明书.md`、`docs/详细设计说明书.md` | 线程模型图含 `blufi` | 5-1 | ⬜ |

---

## 执行顺序建议

```
P0-1 ~ P0-6（登板排查 + 抓包，尽量并行）
      │
      │  以下三个「无依赖」阶段可立即开工：
      ├── 阶段 1（骨架配置）
      ├── 阶段 2（BluFi 协议栈，纯逻辑可单测）
      ├── 阶段 3（wpa_cli 封装）
      │
      ├── 阶段 4（BlueZ D-Bus，依赖 P0-1 服务归属、P0-3/P0-4 环境）
      │        └── 阶段 5（状态机编排，依赖 P0-2 安全模式、P0-6 RSSI 口径）
      │                 └── 阶段 6 → 阶段 7
```

**建议起手**：阶段 1 + 2 + 3 并行推进（完全不依赖 P0，工作量占比约 40%，且能提前把协议栈和 `wpa_cli` 单测跑绿）。P0-1 结论出来后再动阶段 4，避免注册冲突导致返工。

---

## 进度记录（2026-09-05）

阶段 1/2/3 已实现并通过 host `cargo check`（无警告）+ `cargo test --lib blufi`（**13 用例全过**）。

实现映射（与原任务项对应，部分文件组织略有调整）：
- **阶段 1**：1-1 ✅ `src/blufi/mod.rs` + `src/lib.rs` `pub mod blufi`；1-2 ✅ `BluFiConfig` 定义于 `blufi/mod.rs`（非单独 config.rs），含默认值与 `serde(default)` 缺省加载（`scan_timeout_ms<10000` 边界校验暂未做，靠 default 保证合法）；1-3 ✅ 旧配置不动即兼容（`#[serde(default)]`）；1-4 ✅ `BlufiCmd`/`BlufiEvent` 定义 + 通道；1-5 ⬜ 留阶段 6。
- **阶段 2**：2-1~2-4 ✅ `frame.rs`（编解码 / 分片重组 / 发送分片 / ACK，含单测）；2-5 ✅ `SecurityMode` 枚举 + L0 明文 + 非 L0 告警（位于 `blufi/mod.rs`，未单列 `security.rs`）；2-6/2-7 ⬜ P0-2 判定为 L0 明文后不需要。
- **阶段 3**：3-1~3-7 ✅ `wifi.rs`（命令封装 / scan / 解析 / status / probe 探测；`connect` 已改为「写 `/etc/wpa_supplicant.conf` + 重载 wpa_supplicant」而非 wpa_cli add_network 方案），运行期才调用 wpa_cli，不影响编译。

待办（依赖外部）：阶段 4（GATT/D-Bus，需引入 `dbus` 依赖 + 板子实测）、阶段 5 编排已在 `mod.rs` 主循环落地（依赖阶段 4 的 BleLink 注入）、阶段 6（main.rs 装配）、阶段 7 真机联调。

与详细设计的偏差：`BluFiWorker::spawn_with_link(cfg, device_id, cmd_rx, event_tx, link)` 为 5 参（多出 `Box<dyn BleLink>`），因阶段 4 前无 `GattApp`；阶段 4 实现 `GattApp` 后由 `main.rs` 构造传入。
