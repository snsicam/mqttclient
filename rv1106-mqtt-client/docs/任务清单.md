# rv1106-mqtt-client 任务清单（TASKS）

> 关联文档：`概要设计说明书.md`（D16 共享目录）、`详细设计说明书.md`、`测试计划.md`。
> 跨项目架构复审见 `klipper_screen/docs/SPC.md` §10（AV-1~AV-7 / OQ-U1~OQ-U9）。`[x]` = 完成。

---

## 一、已完成（下载 / UDS / 协议对齐）

- [x] **download 落盘「共享目录」方案 A（D16）**：写入 Moonraker `gcodes` 受监控目录、`file_type` 仅 0（非法返回 `err_code=4`）、删除 `server.files.upload`
- [x] **UDS 方法集收敛**为 `ping`/`bind_status`/`download`/`unbind`（`file_list`/`gcode` 移除）
- [x] **AV-1 download 忙闲守卫**：MQTT `download_begin` 与 UDS `download` 共用 `UiState.downloading`，忙回 `err_code=1`
- [x] **download_begin 回复**（协议 §5.5，此前缺失）+ **begin/end 配对**（MQTT/UDS 两路径）
- [x] **errCode 裁剪**到协议范围 0/1/2（内部 3/4 → 1）

## 二、已完成（架构整改，来自 SPC §10）

- [x] **AV-4 / OQ-U1** 下载目录校验：`download()` 前查询 `server.files.roots` 取 `gcodes` 根并缓存（`downlink.rs::gcode_root`），`download.dir` 不在其内则告警并回 `err_code=2`（不再静默成功）；查询不到根时放行并告警、下次重试
- [x] **AV-3 / OQ-U8** UDS 权限：socket 目录 `0770`、socket 文件 `0660`（组归属由部署侧 systemd `Group=`/`UMask=` 保证两进程同组）

## 三、待办（架构整改）

- [ ] **AV-2 / OQ-U4 / OQ-U9** 订阅对象集裁剪/去重：当前 `moonraker.rs::SUBSCRIBE_OBJECTS` 为 12 个对象，需与 klipper_screen 协调各自最小集，避免同机双 WS 重复订阅（RV1106 资源紧张）
- [ ] **OQ-U5** `file_type` 仅 0 的限制与云端确认（协议字段语义对齐）
- [ ] **OQ-U6** UDS `dest` 语义：文档说明 UI 不依赖（跨进程绝对路径），UI 改用 `server.files.list` 轮询

## 四、其他（来自测试计划，未执行）

- [ ] L3 集成测试：本地 Broker（mosquitto/emqx）+ Mock 云端脚本
- [ ] L4 实机测试：RV1106 板 + 云平台
- [ ] L5 负向/异常测试

---

> 变更记录：2026-09-11 建档（AV-3/AV-4 落地、AV-2 待办）；下载协议对齐（begin/end、errCode）与 UDS 收敛已闭环。
